# Multileg RFQ Orchestrator with GUI for Windows

the batch file install necessary dependencies for the GUI program. run python multileg_rfq_orchestrator_GUI_WIN.py 
from the powershell opened in the location where all .py files and the .env file are present. 
this will open a user interface to edit, price and execute RFQs (no portfolio capability included yet). 
